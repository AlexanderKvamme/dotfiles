# Create a new directory and enter it
function mk() {
  mkdir -p "$@" && cd "$@"
}

function settitle()
{
    if [ $# -eq 0 ]
        then
        eval set -- "\\u@\\h: \\w"
    fi

    case $TERM in
        xterm*) local title="\[\033]0;$@\007\]";;
        *) local title=''
    esac
    local prompt=$(echo "$PS1" | sed -e 's/\\\[\\033\]0;.*\\007\\\]//')
    PS1="${title}${prompt}"
}


function donow() 
{
    vim ~/Dropbox/journals/donow.md
}

function dump()
{
    vim ~/Dropbox/journals/dump.md
}

function tdtd()
{
    settitle TDTD
    vim ~/Dropbox/journals/TDTD.md
    settitle TDTD
}

